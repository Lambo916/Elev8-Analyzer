{
  "version": 2,
  "buildCommand": "echo 'No build step required for static HTML'",
  "outputDirectory": "public",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.ts"
    }
  ]
}
